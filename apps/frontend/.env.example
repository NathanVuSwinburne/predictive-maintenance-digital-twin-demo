# Use the deterministic frontend-only portfolio provider (no backend required).
NEXT_PUBLIC_DEMO_MODE=false

# FastAPI mode for local Docker/development.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
SERVER_API_BASE_URL=http://backend:8000
